apiVersion: v1
kind: Service
metadata:
  name: go-ingestor
  labels:
    app: go-ingestor
spec:
  type: ClusterIP
  selector:
    app: go-ingestor
  ports:
    - name: grpc
      port: 50051
      targetPort: grpc
      protocol: TCP
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
